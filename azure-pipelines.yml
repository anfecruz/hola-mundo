trigger:
  - develop
  - main

pool:
  name: Default
  demands:
    - Agent.Name -equals FELIPECRUZ

jobs:
- job: TestJob
  displayName: 'Ejecutar Pruebas E2E'
  timeoutInMinutes: 15
  steps:
  - task: NodeTool@0
    inputs:
      versionSpec: '16.x'
    displayName: 'Instalar Node.js'
    retryCountOnTaskFailure: 3

  - script: |
      npm ci --no-audit --no-fund --prefer-offline
      npm install -g wait-on
    displayName: 'Instalar dependencias'
    timeoutInMinutes: 5

  - script: |
      npm install -D @playwright/test
      npx playwright install chromium --with-deps
    displayName: 'Instalar Playwright'
    timeoutInMinutes: 5

  - script: |
      start /B ng serve --configuration=development
      wait-on http://localhost:4200 -t 60000
      npx playwright test --project=chromium --workers=1 --retries=2 --timeout=30000
      taskkill /F /IM node.exe
    displayName: 'Ejecutar pruebas E2E'
    timeoutInMinutes: 10